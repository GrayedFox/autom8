// This is where you want global before/after hooks and things which need to access the Cypress
// test runner that don't belong in an individual spec

// Import commands.js using ES2015 syntax:
import './commands'

module.exports = () => {
  // cy.before(() => { })
  // cy.beforeEach(() => { })
  // cy.afterEach(() => { })
  // cy.after(() => { })
}
